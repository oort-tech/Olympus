import { TestSolo } from '../modules/TestSolo';
import { address } from '../../src/types';
export declare function setupMarkets(solo: TestSolo, accounts: address[], numMarkets?: number): Promise<void>;
