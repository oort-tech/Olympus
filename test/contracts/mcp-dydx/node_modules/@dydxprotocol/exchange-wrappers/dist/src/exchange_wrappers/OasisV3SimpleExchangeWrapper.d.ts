import { OasisV3Order } from '../types';
export declare class OasisV3SimpleExchangeWrapper {
    private networkId;
    constructor(networkId: number);
    setNetworkId(networkId: number): void;
    getAddress(): string;
    orderToBytes(order: OasisV3Order): number[];
    private toBytes;
}
