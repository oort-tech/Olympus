import { SaiDaiOrder } from '../types';
export declare class SaiDaiExchangeWrapper {
    private networkId;
    constructor(networkId: number);
    setNetworkId(networkId: number): void;
    getAddress(): string;
    orderToBytes(order: SaiDaiOrder): number[];
}
