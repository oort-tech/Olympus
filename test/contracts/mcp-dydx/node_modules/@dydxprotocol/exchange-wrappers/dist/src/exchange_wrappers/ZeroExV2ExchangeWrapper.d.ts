import { ZeroExV2Order } from '../types';
export declare class ZeroExV2ExchangeWrapper {
    private networkId;
    constructor(networkId: number);
    getAddress(): string;
    setNetworkId(networkId: number): void;
    orderToBytes(order: ZeroExV2Order): number[];
    private toBytes;
}
