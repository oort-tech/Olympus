import { OasisV1Order } from '../types';
export declare class OasisV1SimpleExchangeWrapper {
    private networkId;
    constructor(networkId: number);
    setNetworkId(networkId: number): void;
    getAddress(): string;
    orderToBytes(order: OasisV1Order): number[];
    private toBytes;
}
