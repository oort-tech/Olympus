import { OasisV2Order } from '../types';
export declare class OasisV2SimpleExchangeWrapper {
    private networkId;
    constructor(networkId: number);
    setNetworkId(networkId: number): void;
    getAddress(): string;
    orderToBytes(order: OasisV2Order): number[];
    private toBytes;
}
