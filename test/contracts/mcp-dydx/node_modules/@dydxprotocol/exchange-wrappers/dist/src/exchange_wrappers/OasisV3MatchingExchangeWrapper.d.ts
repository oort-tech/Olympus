import { OasisV3MarketOrder } from '../types';
export declare class OasisV3MatchingExchangeWrapper {
    private networkId;
    constructor(networkId: number);
    setNetworkId(networkId: number): void;
    getAddress(): string;
    orderToBytes(order: OasisV3MarketOrder): number[];
    private toBytes;
}
