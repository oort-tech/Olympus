import { TestOrder } from '../types';
export declare class TestExchangeWrapper {
    private networkId;
    constructor(networkId: number);
    getAddress(): string;
    setNetworkId(networkId: number): void;
    orderToBytes(order: TestOrder): number[];
    private toBytes;
}
