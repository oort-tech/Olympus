import { OpenDirectlyOrder } from '../types';
export declare class OpenDirectlyExchangeWrapper {
    private networkId;
    constructor(networkId: number);
    setNetworkId(networkId: number): void;
    getAddress(): string;
    orderToBytes(order: OpenDirectlyOrder): number[];
}
